{
    "name": "@aws/aws-lsp-yaml-json",
    "version": "0.0.1",
    "description": "CodeBuild Language Server for YAML and JSON templates",
    "main": "out/index.js",
    "scripts": {
        "compile": "tsc --build",
        "test": "ts-mocha -b 'src/**/*.test.ts'"
    },
    "dependencies": {
        "@aws/language-server-runtimes": "^0.2.0",
        "@aws/lsp-core": "^0.0.1",
        "@aws/lsp-json-common": "^0.0.1",
        "@aws/lsp-yaml-common": "^0.0.1",
        "vscode-languageserver": "^9.0.1",
        "vscode-languageserver-textdocument": "^1.0.8"
    },
    "prettier": {
        "printWidth": 120,
        "trailingComma": "es5",
        "tabWidth": 4,
        "singleQuote": true,
        "semi": false,
        "bracketSpacing": true,
        "arrowParens": "avoid",
        "endOfLine": "lf"
    }
}
